{
    "session" : {
        "url" : "/favicon.ico?session=#{session}"
    },
    "request" : {
        "url": "/threshold_rules/#{factor}?session=#{session}&burst=#{burst}",
        "method": "DELETE"
    }
}