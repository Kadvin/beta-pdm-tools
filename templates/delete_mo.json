{
    "session" : {
        "url" : "/favicon.ico?session=#{session}"
    },
    "request" : {
        "url": "/mos/#{factor}?session=#{session}&burst=#{burst}",
        "method": "DELETE"
    }
}