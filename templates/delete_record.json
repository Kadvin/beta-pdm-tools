{
    "session" : {
        "url" : "/favicon.ico?session=#{session}"
    },
    "request" : {
        "url": "/record_rules/#{factor}?session=#{session}&burst=#{burst}",
        "method": "DELETE"
    }
}